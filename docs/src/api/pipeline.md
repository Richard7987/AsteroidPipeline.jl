# Pipeline

`run_pipeline` and `search_field`, the end-to-end entry points that wire
detection, (optional) ZOGY differencing, linking, variable-source search,
and astrometric calibration together over a sequence of FITS frames.

```@autodocs
Modules = [AsteroidPipeline]
Pages = ["pipeline.jl"]
```
