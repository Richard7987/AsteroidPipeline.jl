# Rotation Period

Forced-position light-curve photometry for a confirmed discovery
(`light_curve`) and Lomb-Scargle period recovery over it
(`recover_rotation_period`).

```@autodocs
Modules = [AsteroidPipeline]
Pages = ["rotation.jl"]
```
