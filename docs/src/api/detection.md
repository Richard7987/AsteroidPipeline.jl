# Detection & Linking

Per-frame point-source detection (`detect_sources`) and cross-frame
linear-motion matching into asteroid candidate tracklets
(`link_candidates`).

```@autodocs
Modules = [AsteroidPipeline]
Pages = ["detection.jl", "linking.jl"]
```
