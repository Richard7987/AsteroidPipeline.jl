# Cross-Matching

Separating known objects from candidates that warrant human verification,
against SkyBoT, VSX, and SIMBAD (`crossmatch_catalog`).

```@autodocs
Modules = [AsteroidPipeline]
Pages = ["crossmatch.jl"]
```
