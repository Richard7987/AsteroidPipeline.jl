# Variable Stars

Stationary, flux-varying source detection (`find_variable_sources`) and
its supporting statistics — as opposed to `link_candidates`'s
moving-object search.

```@autodocs
Modules = [AsteroidPipeline]
Pages = ["variables.jl"]
```
